# Local hybrid dev — Postgres + migrate only (API via host uvicorn on :8000, UI via Vite on :5173)
# Usage: docker compose -f docker-compose.yml -f docker-compose.local.yml up -d

services:
  api:
    profiles: ["never"]
  web:
    profiles: ["never"]
