services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: fms
      POSTGRES_PASSWORD: fms
      POSTGRES_DB: fms
    ports:
      - "5432:5432"
    volumes:
      - fms_pgdata:/var/lib/postgresql/data

volumes:
  fms_pgdata:
