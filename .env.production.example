# Copy to .env.production.local for direct API URL (no proxy).
# Usually leave unset — Vercel/Netlify proxy /api to Render instead.

# Full API prefix including /api/v1, e.g.:
# VITE_API_BASE_URL=https://fms-demo-api.onrender.com/api/v1
